            // The agent needs to drop the diamond on the pressure plate to open the door
            item.setPos(Vec3.atCenterOf(helper.absolutePos(pressurePlatePos).offset(0, 1, 0)));


open_door passed! (266ms)