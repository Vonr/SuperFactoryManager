name "Patterns"

every 20 ticks do
    -- resources can use regex to match identifiers

    -- when not quoted, asterisks in identifiers get converted to ".*"
    INPUT *ingot* from a
    -- quoted identifiers directly use regex
    OUTPUT ".*ingot.*" to b
end