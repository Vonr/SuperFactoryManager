NAME "known issues"

-- Sometimes managers stop working
---- use "/sfm bust_cable_network_cache" to hopefully fix
-- Having like 9000 labels in a label gun will corrupt your world because the NBT is too big
---- this probably happens with the network tool if you're near a similar situation
-- mouse drag selection desyncs with mouse position in long scripts